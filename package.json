{
  "name": "react-native-apollo-devtools",
  "version": "1.0.0",
  "description": "Apollo devtools for React Native",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run --parallel start",
    "postinstall": "npx patch-package"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.5.5"
  }
}
